var highScoreEl = document.querySelector("#high-score-button");
var startQuizEl = document.querySelector("#start-button");

var quiz = function() {
    // event.preventDefault();
    // resetDisplay();    
    alert("button clicked");
};


    // click for high score
// highScoreEl.addEventListener("submit", function();

    // cick to start quiz
startQuizEl.addEventListener("click", quiz);